var number 

function add(x, y) {
  return x + y;
}

function subtract(x, y) {
  return x - y;
}

function multiply(x, y) {
  return x * y;
}

function divide(x, y) {
  return x / y;
}

function inc(n) {
  number = n + 1;
  return number;
}

function dec(n) {
  number = n - 1;
  return number;
}

function makeInt(n) {
  number = n.parseInt;
  return number;
}

function preserveDecimal(n) {
  number = n.parseFloat;
  return number;
}


